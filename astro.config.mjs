// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // 公開URLに合わせて site を書き換えてください（例: 'https://satsuki.studio'）
  site: 'https://example.com',
});
